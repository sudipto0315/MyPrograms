#include <stdio.h>

int main() {
    char *p = "Hello", q[20] = "world";

    //int *w = (int *) q;
    //*w = 'X';
    //puts(q);
    //for (int i = 0; i < 8; i++)
        //printf("%p contains %c (ASCII: %d).\n", q + i, q[i], q[i]);
    char *s1[2] = {"Vikash", "Shaurya"};
    char  s2[2][20] = {"Vikash", "Shaurya"};
}
